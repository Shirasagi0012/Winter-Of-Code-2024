<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SastImg.Client.Views.TestView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SastImg.Client.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid >
        <Border BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Margin="40,40" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"  Padding="12" >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Images"  Style="{ThemeResource TitleTextBlockStyle}" Margin="24,24"/>
                    <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Images, Mode=OneWay}" 
                              SelectedItem="{x:Bind ViewModel.SelectedImage, Mode=TwoWay}" 
                              x:Name="image_list" 
                              ItemClick="image_list_ItemClick" IsItemClickEnabled="True">
                        <ListView.ItemTemplate>
                            <DataTemplate xmlns:model="using:SastImg.Client.Model"  x:DataType="model:ImageModel">
                                <StackPanel Margin="10,14" Orientation="Horizontal" Spacing="16">
                                    <TextBlock Text="{x:Bind Title}" VerticalAlignment="Center" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                                    <TextBlock Text="{x:Bind Album}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
                <StackPanel Grid.Column="1" Spacing="4" Margin="40,40">
                    <TextBlock Text="Image Details"  Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,24"/>
                    <TextBlock Text="{x:Bind ViewModel.SelectedImage.Title, Mode=OneWay}" Style="{ThemeResource TitleTextBlockStyle}"/>
                    <Grid RowSpacing="4" ColumnSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Album"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind ViewModel.SelectedImage.Album, Mode=OneWay}"/>
                        
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Uploader"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind ViewModel.SelectedImage.Uploader, Mode=OneWay}"/>
                        
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Uploaded At"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind ViewModel.SelectedImage.UploadedAt, Mode=OneWay}"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>
